<div class="container">
  <%= form_for(@tool) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name  %>
    <br>

    <%= f.label :use %>
    <%= f.text_field :use %>
    <br>

    <%= f.submit "Submit", class: "waves-effect btn" %>
  <% end  %>
</div>
